{
  "frontend/src/App.tsx": "Main application component with routing, authentication flow, and approval gate; after login it checks `isCallerApproved` and auto-requests approval for new users, showing the WaitingApproval page for non-approved users before allowing access to the app or profile setup.",
  "frontend/src/components/Layout.tsx": "Root layout component with header, sidebar (with isOpen prop), and an ErrorBoundary-wrapped Outlet for nested routes.",
  "frontend/src/components/Header.tsx": "Top navigation bar with search, notification bell icon showing unread count badge, theme toggle, and user profile dropdown, notification bell scrolls to notifications section on Home page, fixed to use correct hook name useGetUnreadNotificationsCount.",
  "frontend/src/components/Sidebar.tsx": "Left sidebar navigation with nav items including a new \"Favorites\" link (Star icon) placed immediately below \"My Files\", using useNavigate/useRouterState from TanStack Router with active state highlighting.",
  "frontend/src/components/StorageQuotaIndicator.tsx": "Compact storage usage indicator for the sidebar with skeleton loader while loading and a minimal error state that doesn't break the sidebar layout.",
  "frontend/src/components/ProfileSetup.tsx": "Profile setup form for first-time users to enter their display name and optional email, with loading state and error handling.",
  "frontend/src/components/FilePreview.tsx": "File preview modal supporting TXT, images, videos, and PDF with download functionality, fixed TypeScript type conversion for Uint8Array.",
  "frontend/src/components/FileList.tsx": "Self-fetching file and folder listing component with list/grid views, search/filter/sort, pagination, favorites toggling, bulk actions, and per-item menus; lock/unlock icon is on the right side before the three-dot menu, and empty interface declarations replaced with type aliases to fix the no-empty-object-type lint errors.",
  "frontend/src/components/FileUpload.tsx": "Drag-and-drop file upload component with multi-file selection, chunked upload support, and folder context awareness for uploading files directly into the current folder.",
  "frontend/src/components/ActivityFeed.tsx": "Displays recent file activities using useGetRecentActivities hook with icons, action labels, timestamps in scrollable area, shows empty state when no activities exist.",
  "frontend/src/components/UserStorageManager.tsx": "Admin component for managing per-user storage quotas displayed as a table with inline editable numeric input fields pre-filled with the current quota (defaulting to 953.67 MB); each row has a Save button that activates only when the value is changed and calls the setUserQuota mutation with a loading spinner.",
  "frontend/src/pages/LoginPage.tsx": "Landing page with Internet Identity login and feature highlights.",
  "frontend/src/pages/Dashboard.tsx": "Main files dashboard using correct FileList props (onFolderClick), FileUpload, FolderBreadcrumbs, and CreateFolderDialog with parentFolderId support for nested folder creation.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin-only dashboard with three stat cards at the top (Total Users, Pending Approvals, Approved Users) followed by three tabs — User Management (Pending Registrations + All Registrations table with Reject hidden for admin's own row), Storage Management (UserStorageManager component), and Recent Activity (login-only activity log from getLoginLogTable).",
  "frontend/src/pages/Settings.tsx": "Settings page with account info, trash retention period configuration, theme selection, notifications, and security options, fixed to pass Principal object directly instead of string to setUserRetentionPeriod mutation.",
  "frontend/src/hooks/useQueries.ts": "Centralized React Query hooks for all backend operations; `useGetRecentActivities` and `useGetSmartSuggestions` now accept an optional `limit` parameter, and `useListTrashFiles`/`useListTrashFolders` accept an optional `ownerFilter` parameter — fixing the TypeScript errors while preserving all existing call sites and adding the new `useGetLoginLogTable` hook.",
  "frontend/src/lib/utils.ts": "Utility function for merging Tailwind CSS classes.",
  "frontend/src/index.css": "Global styles with OKLCH color system for light and dark modes with blue/purple gradient theme.",
  "frontend/tailwind.config.js": "Tailwind configuration with custom color tokens and design system.",
  "frontend/src/pages/Trash.tsx": "Trash management page with Files/Folders tabs, sorting, pagination, and bulk actions; fixed all four TypeScript errors by using the correct prop names `selectedItems` and `onRestore` for `RestoreDialog`, and `selectedItems` for both `DeleteConfirmationDialog` and `DeleteFolderConfirmationDialog`, removing the non-existent `isLoading` props from dialogs that don't accept them.",
  "frontend/src/components/TrashFilters.tsx": "Advanced filter controls for the Trash page; now imports TrashMetadata directly from backend types and uses @icp-sdk/core/principal for Principal.",
  "frontend/src/components/TrashStorageIndicator.tsx": "Visual storage usage indicator using useGetTrashStorageUsage hook showing total size of trashed files and percentage of quota used.",
  "frontend/src/components/RestoreDialog.tsx": "Dialog component for restoring both files and folders with options to restore to original location or select a different folder, displays appropriate icons and messaging based on item types.",
  "frontend/src/components/DeleteConfirmationDialog.tsx": "Confirmation dialog for permanent file deletion; now imports TrashMetadata directly from backend types instead of the removed TrashItem re-export from useQueries.",
  "frontend/src/components/DeleteFileDialog.tsx": "Dialog component for file deletion that allows users to specify custom retention period before moving to Trash, updated Tailwind CSS class from flex-shrink-0 to shrink-0.",
  "frontend/src/pages/Home.tsx": "Home dashboard with responsive grid layout; each section (storage, pinned files, activity, notifications, suggestions) renders independently with its own loading/error states so slow queries don't block the page.",
  "frontend/src/components/StorageOverview.tsx": "Storage usage visualization card with skeleton loader while the quota query is loading and an inline error state if the query fails.",
  "frontend/src/components/QuickActions.tsx": "Three prominent action buttons (Upload Files, New Folder, Create Text Document) with modern card styling, icons, hover effects, and functional implementations that track uploads in the recent uploads context, now properly wired to CreateFolderDialog with currentFolderId support for nested folder creation.",
  "frontend/src/components/RecentActivityCard.tsx": "Displays recent file activities with skeleton loaders while loading, inline error state on failure, and empty state when no activities exist.",
  "frontend/src/components/PinnedFiles.tsx": "Displays favorited files in a horizontal scrollable layout with skeleton loaders while loading and graceful error/empty states.",
  "frontend/src/components/SharedWithMe.tsx": "Shows files shared with the current user with search filtering, per-row checkboxes, master checkbox, selected count label, bulk action bar (Download, Copy Names), and PaginationControls using the correct usePagination hook API (paginatedData as function, no totalPages prop passed).",
  "frontend/src/components/Notifications.tsx": "Displays user notifications with skeleton loaders while loading, inline error state on failure, and empty state when no notifications exist; auto-refreshes every 10 seconds.",
  "frontend/src/components/SmartSuggestions.tsx": "Displays frequently accessed file suggestions with skeleton loaders while loading and inline error/empty states that don't block other components.",
  "frontend/src/hooks/useFileSearch.ts": "Custom hook that manages file search state with debounced search query and filters files by name and type (extension).",
  "frontend/src/hooks/useFileFilters.ts": "Custom hook that manages file filter state (name, type, date range, size range) and provides filtering logic for FileMetadata arrays with active filter count tracking.",
  "frontend/src/hooks/useFileSorting.ts": "Custom hook that manages file sorting state (sort by name/date/size/type with ascending/descending order) and provides sorting logic for FileMetadata arrays.",
  "frontend/src/components/FileSearchInput.tsx": "Search input component with debounced input (300ms delay), search icon, and clear button for real-time file searching.",
  "frontend/src/components/FileFilterDropdown.tsx": "Dropdown menu component with comprehensive filter controls for file name, type, date range, and size range with active filter badges and clear all functionality.",
  "frontend/src/components/FileSortDropdown.tsx": "Dropdown menu component for file sorting options (name, date, size, type) with visual indicators for active sort and sort direction (ascending/descending).",
  "frontend/src/components/FileToolbar.tsx": "Toolbar component that groups search input, filter dropdown, sort dropdown, and view mode toggle in a responsive layout for the file list interface.",
  "frontend/src/pages/Shared.tsx": "Shared files page with two tabs (Shared With Me / Shared By Me) using shadcn Tabs, each rendering its own component with independent pagination state.",
  "frontend/src/pages/Recent.tsx": "Recent uploads page with FileToolbar using correct individual filter props, per-item checkboxes, master checkbox, selected count label, bulk action bar (Download, Copy Names, Delete), grid/list view modes with RecentFileCard/RecentFileRow sub-components, PaginationControls, FilePreview (with fileData), and DeleteFileDialog (with onConfirm).",
  "frontend/src/contexts/RecentUploadsContext.tsx": "React context provider that manages recently uploaded files in the current session using local state, provides functions to add uploads and clear the list without persistence.",
  "frontend/src/components/CreateFolderDialog.tsx": "Modal dialog component for creating new folders with name input validation, error handling, and loading state during folder creation, fixed to use parentId instead of parentFolderId.",
  "frontend/src/components/MoveToFolderDialog.tsx": "Dialog component that displays available folders in a scrollable list with radio selection for moving files to a destination folder.",
  "frontend/src/components/FolderBreadcrumbs.tsx": "Breadcrumb navigation component showing the current folder path with recursive parent traversal; updated to accept `string | null` for `currentFolderId` and uses `useGetFolder` with the corrected nullable signature.",
  "frontend/src/components/RenameFolderDialog.tsx": "Modal dialog component for renaming folders with name input validation, error handling, and loading state during rename operation.",
  "frontend/src/components/MoveFolderDialog.tsx": "Dialog component for moving folders with enhanced error handling that displays actual backend error messages in toast notifications and only shows success when backend confirms successful move operation.",
  "frontend/src/components/DeleteFolderDialog.tsx": "New confirmation dialog for moving a folder to trash; accepts a `Folder` object and uses `useDeleteFolderToTrash` mutation with loading state and error handling.",
  "frontend/src/components/DeleteFolderConfirmationDialog.tsx": "Confirmation dialog specifically for permanently deleting folders from trash, displays folder names, original paths, and warns that all contents will be permanently deleted.",
  "frontend/src/components/DeleteFolderToTrashDialog.tsx": "Dialog component for moving folders to trash with retention period selection (30, 60, or 90 days), displays folder name and warns about moving all contents to trash, converts days to nanoseconds before calling backend.",
  "frontend/src/hooks/usePagination.ts": "Custom React hook managing pagination state (currentPage defaulting to 1, itemsPerPage defaulting to 10, not persisted) with setPage, setItemsPerPage, resetPage, and paginatedData helpers for slicing arrays.",
  "frontend/src/components/PaginationControls.tsx": "Reusable pagination UI component with items-per-page Select (10/25/50/100), First/Last buttons, Previous/Next buttons, page number links with ellipsis, and a \"Showing X–Y of Z items\" range indicator.",
  "frontend/src/components/SharedByMe.tsx": "Shows files shared by the current user with search filtering, per-row checkboxes, master checkbox, selected count label, bulk Revoke Shares action bar, and PaginationControls using the correct usePagination hook API.",
  "frontend/src/components/ErrorBoundary.tsx": "React class-based error boundary that catches rendering errors in child components and displays a user-friendly error message with Try Again and Refresh Page buttons.",
  "frontend/src/components/LoadingTimeout.tsx": "Wrapper component that shows a timeout fallback UI after a configurable duration (default 45 seconds, increased from 15s) if the app hasn't resolved; only intended for auth/actor initialization, not data fetching.",
  "frontend/src/components/RenameFileDialog.tsx": "New dialog component for renaming a file; accepts fileId and currentName props, pre-fills the input, validates non-empty name, and shows an informative error toast since the backend does not yet expose a renameFile endpoint.",
  "frontend/src/components/FolderProtectionModal.tsx": "Modal for configuring folder protection settings; now includes a \"Current Password\" verification field (shown only when the folder already has protection) that must be correctly verified before toggling the lock switch, removing protection, or updating the password — with inline error/success feedback and real-time SHA-256 hash comparison.",
  "frontend/src/components/FolderPasswordPrompt.tsx": "Modal for entering a password to access a locked folder; tracks failed attempts (max 5), shows remaining attempts, locks out the folder in sessionStorage after 5 failures — removed the non-existent `useAddNotification` import and the admin-only notification call that would fail for regular users.",
  "frontend/src/components/ConnectionError.tsx": "New error state component displayed when the backend actor connection times out; shows a descriptive message, a \"Retry Connection\" button that triggers re-initialization via the `onRetry` callback, and a \"Refresh Page\" fallback button.",
  "frontend/src/components/BulkDeleteDialog.tsx": "Confirmation dialog for bulk file deletion accepting fileCount, isOpen, onClose, onConfirm, and isLoading props; displays a retention period selector (30/60/90 days) using RadioGroup and AlertDialog styling.",
  "frontend/src/components/BulkShareDialog.tsx": "Dialog for bulk sharing selected files with another user; collects recipient principal ID, view/edit/download permissions, and optional message, then calls shareFileMutation for each file using Promise.allSettled with toast notifications.",
  "frontend/src/components/FolderActionPasswordPrompt.tsx": "Reusable modal dialog that requires password confirmation before performing Rename, Move, or Delete actions on password-protected folders; hashes input with SHA-256, tracks up to 5 failed attempts with session-storage lockout, resets state on open, and calls onSuccess/onClose callbacks appropriately.",
  "frontend/src/pages/Favorites.tsx": "Favorites page displaying the user's starred files with loading skeletons, empty state, file name/size/type display, and preview/download/remove-favorite actions; includes required fileData prop for FilePreview.",
  "frontend/src/pages/WaitingApproval.tsx": "Waiting screen shown to authenticated but not-yet-approved users; auto-submits an approval request on mount, shows a pending status card with refresh and logout buttons, with the unused `eslint-disable` directive removed and `identity` added to the effect dependency array."
}